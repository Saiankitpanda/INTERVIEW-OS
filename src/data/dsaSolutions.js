// DSA Solutions — Part 1 (nc-1 to nc-50)
export const DSA_SOLUTIONS = {
  'nc-1': { approach: 'Add each element to a HashSet. If the element already exists, return true.', brute: 'O(n²) — compare every pair', optimal: 'O(n) time, O(n) space — HashSet', why: 'HashSet gives O(1) average lookup vs O(n) for array scan. Sorting would be O(n log n) — overkill here.' },
  'nc-2': { approach: 'Count character frequencies with a map/array. Compare counts of both strings. If all match, they are anagrams.', brute: 'O(n log n) — sort both strings and compare', optimal: 'O(n) time, O(1) space — frequency array of 26 chars', why: 'Sorting is O(n log n). Frequency counting is O(n). Since alphabet is fixed size (26), space is O(1) not O(n).' },
  'nc-3': { approach: 'Use a HashMap to store each number and its index. For each element, check if (target - num) exists in the map.', brute: 'O(n²) — check all pairs with nested loops', optimal: 'O(n) time, O(n) space — one-pass HashMap', why: 'One pass is enough: store complement as you go. Two pointers only works on sorted arrays.' },
  'nc-4': { approach: 'Sort each word to get a canonical key. Group words by this key in a HashMap.', brute: 'O(n² × k) — compare every string pair', optimal: 'O(n × k log k) time where k=max word length', why: 'Sorting each word to form a key is the cleanest grouping strategy. Alternatively use character count as key for O(n×k).' },
  'nc-5': { approach: 'Count frequencies in HashMap. Use a min-heap of size k to track top-k elements. Or use bucket sort on frequency.', brute: 'O(n log n) — sort by frequency', optimal: 'O(n) — bucket sort by frequency (frequency ≤ n)', why: 'Bucket sort beats heap (O(n log k)) because frequency is bounded by n, so we get O(n) buckets.' },
  'nc-6': { approach: 'Encode: prefix each string with its length and a delimiter (e.g. "4#word"). Decode: read the length, skip delimiter, extract substring.', brute: 'Use a special separator — fails if separator appears in string', optimal: 'O(n) encode/decode with length-prefix encoding', why: 'Length-prefix is the only reliable way to encode arbitrary strings without collision.' },
  'nc-7': { approach: 'Two passes: left pass computes prefix products, right pass computes suffix products. Multiply them for each index.', brute: 'O(n²) — for each element, multiply all others', optimal: 'O(n) time, O(1) extra space (output array not counted)', why: 'Division is forbidden. Prefix+suffix trick avoids division and achieves O(n) without extra arrays.' },
  'nc-8': { approach: 'Use three sets (rows, cols, boxes) tracking seen digits. Box index = (r/3)*3 + c/3. Single pass validation.', brute: 'O(n²) per row/col/box check separately', optimal: 'O(81) = O(1) — fixed board size, single pass', why: 'Board is always 9×9. Every operation is O(1) in practice. HashSet per group is cleaner than nested loops.' },
  'nc-9': { approach: 'Add all numbers to a HashSet. For each number, if n-1 is NOT in set, it is a sequence start — count forward from there.', brute: 'O(n log n) — sort then count streaks', optimal: 'O(n) time, O(n) space — HashSet with smart start detection', why: 'Only start counting from sequence beginnings avoids recount. Each element is visited at most twice → O(n).' },
  'nc-10': { approach: 'Two pointers from both ends. Skip non-alphanumeric chars. Compare lowercase chars until pointers meet.', brute: 'O(n) — clean string first, then check', optimal: 'O(n) in-place two pointers — no extra string needed', why: 'In-place two pointers saves O(n) space vs creating a cleaned string. Same time complexity.' },
  'nc-11': { approach: 'Array is sorted. Use two pointers (left=0, right=end). Move left if sum too small, move right if too large.', brute: 'O(n²) — nested loops checking all pairs', optimal: 'O(n) two pointers — sorted array enables convergence', why: 'Sorting enables two pointer convergence. Only works because array is already sorted. Binary search would be O(n log n).' },
  'nc-12': { approach: 'Sort array. For each element i, use two pointers on the rest. Skip duplicates to avoid repeated triplets.', brute: 'O(n³) — check all triplets', optimal: 'O(n²) time — sort + two pointers per element', why: 'Sorting costs O(n log n) but enables O(n) two-pointer per element → O(n²) total. Hash-based approaches struggle with deduplication.' },
  'nc-13': { approach: 'Two pointers at both ends. Area = min(h[l],h[r]) × (r-l). Move the shorter side inward (moving taller side can only decrease area).', brute: 'O(n²) — check all pairs', optimal: 'O(n) — two pointers with greedy height choice', why: 'Moving the taller side inward guarantees we never miss a better solution. Width decreases, so only increasing height can help.' },
  'nc-14': { approach: 'Two pointers. For each position, water = min(maxLeft, maxRight) - height[i]. Track running max from both sides.', brute: 'O(n²) — for each bar, scan left and right for max', optimal: 'O(n) time, O(1) space — two pointers with running max', why: 'Precomputing left/right max arrays is O(n) space. Two-pointer eliminates the arrays: the pointer on the shorter side can be processed immediately.' },
  'nc-15': { approach: 'Track minPrice seen so far. For each price, compute profit = price - minPrice. Track maxProfit.', brute: 'O(n²) — try every buy-sell pair', optimal: 'O(n) time, O(1) space — one pass with running min', why: 'One pass is enough: the max profit with buy on day i is always price[i] - min(price[0..i-1]).' },
  'nc-16': { approach: 'Sliding window with a HashSet. Expand right pointer, shrink left when duplicate found. Track max window size.', brute: 'O(n²) or O(n³) — check all substrings', optimal: 'O(n) — sliding window with HashMap for O(1) jump', why: 'HashMap stores last seen index so we can jump left pointer directly instead of shrinking one by one.' },
  'nc-17': { approach: 'Sliding window. Track max frequency char in window. If (window_size - max_freq) > k, shrink window from left.', brute: 'O(n²) — check all substrings', optimal: 'O(n) — sliding window, only grow window size, never shrink below best', why: 'Key insight: we never need to shrink the window below the best found so far. Window size is monotonically non-decreasing.' },
  'nc-18': { approach: 'Fixed-size sliding window. Use frequency arrays for s1 and current window. Compare arrays as window slides.', brute: 'O(n×m) — check every substring of length |s1|', optimal: 'O(n) — sliding window with array comparison O(26)=O(1)', why: 'Since alphabet is fixed at 26 chars, array comparison is O(1). Sliding avoids recomputing from scratch each step.' },
  'nc-19': { approach: 'Sliding window with two frequency maps. Expand right, contract left when all t chars are satisfied. Track min window.', brute: 'O(n²) — try all substrings', optimal: 'O(n+m) — each char added/removed at most once', why: 'The key is the "formed" counter — we only count chars that reach required frequency, enabling O(1) window validity check.' },
  'nc-20': { approach: 'Use a monotonic deque (decreasing). Remove elements out of window from front, remove smaller elements from back before adding new.', brute: 'O(n×k) — scan each window for max', optimal: 'O(n) — each element added/removed from deque once', why: 'Deque maintains decreasing order so front is always the max. Each element enters and leaves deque at most once → O(n) total.' },
  'nc-21': { approach: 'Stack. Push opening brackets. On closing bracket, check if top of stack matches. Return true if stack empty at end.', brute: 'O(n²) — find and remove matched pairs repeatedly', optimal: 'O(n) time, O(n) space — single stack pass', why: 'Stack perfectly models the LIFO nesting requirement. One pass is optimal since we must examine every char.' },
  'nc-22': { approach: 'Use two stacks: one for values, one for minimums. Push current min to minStack on every push (min of new val vs current min).', brute: 'O(n) getMin — scan entire stack', optimal: 'O(1) all operations — auxiliary min stack', why: 'Storing min at every level ensures O(1) getMin without scanning. Space doubles but that is the standard tradeoff.' },
  'nc-23': { approach: 'Stack. Push numbers. On operator, pop two numbers, compute, push result. Final stack top is answer.', brute: 'N/A — stack is the natural solution for RPN', optimal: 'O(n) time, O(n) space', why: 'RPN was invented to be evaluated with a stack. No better approach exists since every token must be processed.' },
  'nc-24': { approach: 'Backtracking with a stack. Track open and close counts. Add open if open < n, add close if close < open.', brute: 'O(n × 2^2n) — generate all strings, filter valid', optimal: 'O(4^n / sqrt(n)) — Catalan number, only valid sequences generated', why: 'Pruning: only add ) when it matches an unmatched (. This eliminates all invalid branches early.' },
  'nc-25': { approach: 'Monotonic stack (decreasing temperatures). For each day, pop stack while current temp > stack top temp. Answer[popped] = i - popped_index.', brute: 'O(n²) — for each day, scan forward for warmer day', optimal: 'O(n) — each element pushed/popped once', why: 'Monotonic stack defers answering until a warmer day is found. Each element processed exactly twice → O(n).' },
  'nc-26': { approach: 'Sort cars by position (desc). Use a stack tracking fleet arrival times. If current car arrives before or at stack top, it joins that fleet.', brute: 'O(n²) — simulate each car catching each fleet', optimal: 'O(n log n) — sort + single stack pass', why: 'Sorting by position ensures we process cars in order. A car can only catch fleets ahead, never behind.' },
  'nc-27': { approach: 'Monotonic stack (increasing heights). For each bar, when a shorter bar appears, pop and compute area using popped height × distance to new stack top.', brute: 'O(n²) — for each bar, expand left and right', optimal: 'O(n) — each bar pushed/popped once', why: 'The monotonic stack efficiently tracks the "left boundary" for each bar. Width calculation uses the gap to the new stack top.' },
  'nc-28': { approach: 'Classic binary search. Compare mid with target. Move left or right half accordingly.', brute: 'O(n) linear scan', optimal: 'O(log n) — halve search space each step', why: 'Binary search is optimal for sorted arrays — proven lower bound of Ω(log n) for comparison-based search.' },
  'nc-29': { approach: 'Treat the 2D matrix as a 1D sorted array. Map index i → row=i//cols, col=i%cols. Apply binary search.', brute: 'O(m×n) linear scan', optimal: 'O(log(m×n)) — virtual 1D binary search', why: 'Row-sorted + first > last-of-prev means the whole matrix is one sorted sequence. Binary search on virtual 1D index is cleanest.' },
  'nc-30': { approach: 'Binary search on the answer (speed k). Check if a given k allows eating all bananas in h hours. Find min k.', brute: 'O(max(piles) × n) — try every speed from 1 to max', optimal: 'O(n log max(piles)) — binary search on speed', why: 'The feasibility check (can k work?) is monotonic — if k works, k+1 also works. Binary search on monotonic functions is always O(log range).' },
  'nc-31': { approach: 'Binary search. Determine which half is sorted. If target is in sorted half, search there; else search the other half.', brute: 'O(n) — linear scan', optimal: 'O(log n) — one half is always sorted in a rotation', why: 'At least one half is always sorted in a rotated sorted array. This invariant lets us eliminate half the search space per step.' },
  'nc-32': { approach: 'Same as nc-31 but return index of target. If target not in sorted half, search the other. Return -1 if not found.', brute: 'O(n) — linear scan', optimal: 'O(log n) — modified binary search', why: 'Same insight: one half is always sorted. We can check in O(1) if target is in that half, then recurse on appropriate side.' },
  'nc-33': { approach: 'HashMap of key → sorted list of (timestamp, value). Binary search on timestamps for get(key, timestamp).', brute: 'O(n) linear scan for each get', optimal: 'O(log n) per get — binary search on sorted timestamps', why: 'Timestamps are added in increasing order, so the list is always sorted. Binary search (bisect_right - 1) finds the floor timestamp.' },
  'nc-34': { approach: 'Binary search to find partition in smaller array such that left halves of both arrays form the correct median split.', brute: 'O((m+n) log(m+n)) — merge and find median', optimal: 'O(log min(m,n)) — binary search on smaller array partition', why: 'We binary search on partitions of the smaller array. Median condition gives O(1) validation per partition check.' },
  'nc-35': { approach: 'Iterative: use prev, curr, next pointers. Or recursive: reverse rest of list, then fix current link.', brute: 'O(n) — collect into array, rebuild reversed', optimal: 'O(n) time, O(1) space — in-place pointer reversal', why: 'Pointer reversal achieves O(1) space. Array approach wastes O(n) space. Recursive uses O(n) stack space.' },
  'nc-36': { approach: 'Iterative: dummy head, compare heads of both lists, attach smaller. Or recursive: smaller head + merge(rest).', brute: 'O(n+m) — collect both into array, sort, rebuild', optimal: 'O(n+m) time, O(1) space iterative (O(n+m) recursive stack)', why: 'Iterative is preferred over recursive for large lists to avoid stack overflow. Both are O(n+m) time.' },
  'nc-37': { approach: 'Find middle (slow/fast pointers), reverse second half, interleave first and reversed second halves.', brute: 'O(n) space — collect nodes in array, relink', optimal: 'O(n) time, O(1) space — three steps: find mid, reverse, merge', why: 'Finding mid + reversing second half allows O(1) space interleaving. Array approach is simpler but wastes O(n) space.' },
  'nc-38': { approach: 'Two pointers with a gap of n. Move fast pointer n+1 steps ahead. Then move both until fast reaches end. Remove slow.next.', brute: 'Two passes — find length, then remove', optimal: 'O(n) single pass — two pointers with gap n', why: 'One pass saves a constant factor. The n-gap trick positions slow pointer exactly before the target node.' },
  'nc-39': { approach: 'Use a HashMap to map original nodes to copies. First pass: create copies. Second pass: set next and random pointers.', brute: 'O(n²) — for each random pointer, find the copy by scanning', optimal: 'O(n) time, O(n) space — HashMap for O(1) copy lookup', why: 'HashMap gives O(1) lookup for any node copy. The two-pass approach ensures all nodes exist before setting random pointers.' },
  'nc-40': { approach: 'Traverse both lists simultaneously. Add digits plus carry. Create new node for each sum. Handle remaining carry.', brute: 'Convert to integers, add, convert back — fails for very large numbers', optimal: 'O(max(m,n)) — digit by digit simulation', why: 'Integer conversion fails for arbitrarily large numbers. Digit simulation is the correct and only O(max(m,n)) approach.' },
  'nc-41': { approach: "Floyd's cycle detection. Slow moves 1 step, fast moves 2. If they meet, there's a cycle.", brute: 'O(n) space — HashSet of visited nodes', optimal: 'O(n) time, O(1) space — two pointers (Floyd)', why: "Floyd's algorithm uses no extra space. HashSet approach uses O(n) space but both are O(n) time." },
  'nc-42': { approach: "Floyd's cycle detection on the array treated as a linked list. Array values are 'next' pointers. Find cycle entry = duplicate.", brute: 'O(n) space — HashSet, or O(n log n) — sort', optimal: 'O(n) time, O(1) space — Floyd on implicit linked list', why: 'Constraint: cannot modify array, O(1) space required. Floyd treats indices as a linked list where value = next pointer.' },
  'nc-43': { approach: 'Doubly linked list + HashMap. HashMap: key→node for O(1) access. DLL: maintains recency order. On get/put, move node to head.', brute: 'O(n) — scan list for LRU eviction', optimal: 'O(1) get and put — DLL for order, HashMap for lookup', why: 'HashMap alone cannot evict LRU in O(1). DLL alone cannot find nodes in O(1). Combined: O(1) for both.' },
  'nc-44': { approach: 'Min-heap of (value, list_index, node). Always extract minimum, push next node from same list.', brute: 'Merge two-at-a-time: O(n×k)', optimal: 'O(n log k) — heap always has at most k elements', why: 'Heap size stays at k throughout. Each of the n total nodes is pushed/popped once → O(n log k). Divide-and-conquer also O(n log k).' },
  'nc-45': { approach: 'Find kth node as tail of group. Reverse group by relinking. Connect to next reversed group recursively.', brute: 'O(n) space — collect nodes, reverse in chunks', optimal: 'O(n) time, O(n/k) recursive stack or O(1) iterative', why: 'In-place reversal avoids extra space. Recursive depth is n/k (number of groups). Iterative version is O(1) space.' },
  'nc-46': { approach: 'DFS/BFS. For each node, swap left and right children. Recurse on both children.', brute: 'N/A — recursion IS the solution', optimal: 'O(n) time, O(h) space (h=height) for DFS', why: 'Every node must be visited once. O(n) is optimal. BFS uses O(w) space (w=max width), DFS uses O(h).' },
  'nc-47': { approach: 'DFS: return 1 + max(depth(left), depth(right)). BFS: count levels.', brute: 'N/A — recursion is the natural solution', optimal: 'O(n) time, O(h) space', why: 'Must visit all nodes to find the deepest leaf. O(n) is tight lower bound. DFS uses O(h) stack, BFS uses O(w) queue.' },
  'nc-48': { approach: 'DFS. At each node, compute left_depth + right_depth (path through this node). Track global max. Return max(left,right)+1 up.', brute: 'O(n²) — for each node, find longest path through it', optimal: 'O(n) — single DFS computing depth bottom-up', why: 'Bottom-up DFS computes the diameter passing through each node in O(1) using already-computed subtree depths.' },
  'nc-49': { approach: 'DFS returning height. If subtree is unbalanced, return -1 as sentinel. At each node, check if |left-right| > 1.', brute: 'O(n²) — compute height at every node separately', optimal: 'O(n) — single DFS computing height bottom-up', why: 'The -1 sentinel propagates imbalance upward without re-traversal. Avoids O(n) height calls at every node.' },
  'nc-50': { approach: 'DFS. At each step, check if values match AND both subtrees match. Return false on any mismatch.', brute: 'N/A — recursion is the only natural approach', optimal: 'O(n) time, O(h) space', why: 'Must compare every node. O(n) is optimal. Short-circuit on first mismatch saves time in practice.' },
  'nc-51': { approach: 'For each node in root, check if subtree matches subRoot using isSameTree. DFS over root.', brute: 'O(n×m) — check every root node as potential match', optimal: 'O(n×m) is required; can do O(n+m) with string serialization or hashing', why: 'Naive is O(n×m). KMP/hashing on serialized trees achieves O(n+m) but complexity of implementation rarely justifies it in interviews.' },
  'nc-52': { approach: 'BST property: if both p,q < root go left; if both > root go right; else current node is LCA.', brute: 'O(n) — general LCA without BST property', optimal: 'O(h) time where h=height — O(log n) for balanced BST', why: 'BST property lets us avoid traversing the whole tree. LCA is the first node where p and q diverge to different sides.' },
  'nc-53': { approach: 'BFS with a queue. Process all nodes at current level before moving to next. Collect values per level.', brute: 'DFS with level tracking — same complexity, more complex code', optimal: 'O(n) time, O(w) space where w=max width', why: 'BFS naturally processes level by level. DFS needs extra level parameter. Both are O(n) but BFS is more intuitive here.' },
  'nc-54': { approach: 'BFS level order. For each level, record only the last node value (rightmost). Or DFS tracking depth, update right-side value.', brute: 'O(n) — full BFS/DFS unavoidable', optimal: 'O(n) time, O(w) space BFS or O(h) space DFS', why: 'Every level must be checked since a right subtree can be null. DFS (right before left, track depth) is elegant with O(h) space.' },
  'nc-55': { approach: 'DFS carrying max value seen on path from root. A node is good if node.val >= max_so_far. Count all good nodes.', brute: 'O(n²) — for each node, traverse path from root', optimal: 'O(n) — single DFS passing running max down', why: 'Passing running max down avoids re-traversal. One pass visits each node once.' },
  'nc-56': { approach: 'DFS with (min, max) bounds. Root: (-inf, +inf). Left child: (min, node.val). Right child: (node.val, max). Invalid if value out of bounds.', brute: 'O(n²) — for each node, verify BST property by checking subtree', optimal: 'O(n) — single DFS with bounds propagation', why: 'Bounds propagation is the key insight. Simply checking left < root < right is WRONG (e.g. right subtree of left child must also be < root).' },
  'nc-57': { approach: 'Inorder traversal of BST gives sorted sequence. Count nodes visited; return value at kth count.', brute: 'O(n) — collect all values, sort, return kth', optimal: 'O(h+k) — stop early when kth element found', why: 'BST inorder = sorted. Stop at kth element rather than traversing whole tree. O(h+k) beats O(n) when k is small.' },
  'nc-58': { approach: 'Preorder first element is root. Find root in inorder to split left/right subtrees. Recurse with sliced arrays.', brute: 'O(n²) — linear search for root in inorder each time', optimal: 'O(n) — HashMap for inorder index lookup', why: 'HashMap gives O(1) root index lookup vs O(n) scan. Reduces total from O(n²) to O(n).' },
  'nc-59': { approach: 'DFS returning max gain from each subtree. At each node, candidate path = left_gain + val + right_gain. Update global max.', brute: 'O(n²) — compute path sum for every path', optimal: 'O(n) — bottom-up DFS, each node contributes its best single-branch gain', why: 'Key insight: a path through a node = best left branch + node + best right branch. We pass single-branch max up, not the full path.' },
  'nc-60': { approach: 'Serialize: BFS/DFS, use "null" for missing nodes. Deserialize: reconstruct using same order, tracking null markers.', brute: 'N/A — encode/decode must use a traversal', optimal: 'O(n) time and space for both operations', why: 'Preorder DFS with null markers is simplest. BFS (level-order like LeetCode) also works. Both require O(n) to encode all nodes.' },
  'nc-61': { approach: 'Each TrieNode has children[26] and isEnd flag. Insert: create nodes along path. Search: traverse path, check isEnd. StartsWith: traverse path.', brute: 'HashSet for search/startsWith — but cannot do prefix queries efficiently', optimal: 'O(m) per operation where m=word length', why: 'Trie is optimal for prefix operations. HashSet gives O(1) exact search but O(n×m) for prefix queries.' },
  'nc-62': { approach: 'Trie with dot wildcard. On dot, try all 26 children recursively. On letter, follow that child.', brute: 'O(n×m) — check every stored word for match', optimal: 'O(m×26^d) where d=number of dots — unavoidable for wildcards', why: 'Wildcard forces branching at each dot. Trie limits search to inserted words only vs. full string generation.' },
  'nc-63': { approach: 'Build Trie from word list. DFS on board, following Trie paths. Prune dead branches. Remove found words from Trie.', brute: 'O(n×m×4^L) — for each cell, DFS for each word separately', optimal: 'O(n×m×4^L) worst case but Trie shared prefix pruning reduces constant dramatically', why: 'Trie allows simultaneous search for all words. Removing found words prevents duplicates and prunes further.' },
  'nc-64': { approach: 'Min-heap of size k. On add: push value, if size > k pop minimum. Heap top is kth largest.', brute: 'O(n log n) — sort all elements each time', optimal: 'O(n log k) total — heap maintains only k elements', why: 'Min-heap of size k: everything below kth largest is discarded. Heap top = kth largest. Log k per operation vs log n for full heap.' },
  'nc-65': { approach: 'Max-heap (negate values in languages without one). Pop two heaviest, compute result, push back if nonzero.', brute: 'O(n² log n) — sort each iteration', optimal: 'O(n log n) — heap: each stone processed at most twice', why: 'Heap gives O(log n) pop/push vs O(n) for finding max in array. Total ops are O(n) each at O(log n) = O(n log n).' },
  'nc-66': { approach: 'Min-heap of size k using squared distance as key. Or max-heap of size k — evict farthest when size exceeds k.', brute: 'O(n log n) — sort all points by distance', optimal: 'O(n log k) — maintain heap of size k', why: 'We only need k closest. Max-heap of size k evicts the farthest seen so far. No need to sort all n points.' },
  'nc-67': { approach: 'Quickselect (partition like quicksort, recurse only on the side containing kth largest). Or min-heap of size k.', brute: 'O(n log n) — full sort', optimal: 'O(n) average quickselect, O(n log k) heap', why: 'Quickselect is O(n) average (O(n²) worst). Heap is O(n log k) guaranteed. For interviews, heap is safer; quickselect is impressive.' },
  'nc-68': { approach: 'Count task frequencies. Use max-heap + cooldown queue. Each cycle: pick most frequent available task, cooldown others.', brute: 'O(n × interval) — simulate each time unit naively', optimal: 'O(n log n) — heap-based greedy simulation', why: 'Always scheduling the most frequent available task minimizes idle time. Math formula approach also O(n) but harder to explain.' },
  'nc-69': { approach: 'HashMap userId→followees. HashMap userId→tweet list (with timestamps). getNewsFeed: merge latest tweets from all followees with heap.', brute: 'O(n×m) — scan all tweets per user', optimal: 'O(f log f) per getNewsFeed where f=number of followees', why: 'Heap merges f sorted tweet lists efficiently. Store only last 10 tweets per user to bound space.' },
  'nc-70': { approach: 'Two heaps: max-heap for lower half, min-heap for upper half. Balance sizes to differ by at most 1. Median = top of larger or avg of both tops.', brute: 'O(n log n) — sort on every findMedian call', optimal: 'O(log n) addNum, O(1) findMedian — two heaps', why: 'Two-heap approach maintains balance invariant. The tops of both heaps always give the median. Segment tree is overkill here.' },
  'nc-71': { approach: 'Backtracking: for each element, decide include or exclude. Recurse with next index. Collect result at each call.', brute: 'O(n×2^n) — same as optimal, backtracking IS optimal', optimal: 'O(n×2^n) — 2^n subsets each of up to n elements', why: 'Output size is 2^n (all subsets). Any algorithm must be at least O(2^n). This is output-optimal.' },
  'nc-72': { approach: 'Backtracking. Sort candidates. For each element, include it (can reuse) and recurse; or skip to next element.', brute: 'O(2^target) naive recursion with repetitions', optimal: 'O(n^(target/min_val)) — backtracking with pruning', why: 'Sorting + pruning (skip when remaining < candidate) dramatically reduces branches. Cannot do better since output can be exponential.' },
  'nc-73': { approach: 'Backtracking. Swap current index with each subsequent index. Recurse. Swap back (backtrack).', brute: 'O(n!×n) — same as optimal', optimal: 'O(n×n!) — n! permutations each length n', why: 'n! permutations is the exact output size. Any correct algorithm is Ω(n!). In-place swapping avoids extra space.' },
  'nc-74': { approach: 'Sort first. Backtracking with duplicate skipping: if nums[i] == nums[i-1] and i > start, skip to avoid duplicate subsets.', brute: 'O(n×2^n) generate all, filter duplicates', optimal: 'O(n×2^n) with inline deduplication — fewer actual subsets generated', why: 'Sorting groups duplicates together. Skipping same-value siblings at same recursion depth prevents duplicate subsets cleanly.' },
  'nc-75': { approach: 'Sort. Same as combination sum but skip used elements and skip duplicates (if candidates[i]==candidates[i-1] and i>start, skip).', brute: 'Generate all, filter by sum and dedup', optimal: 'O(2^n) with sorting+pruning', why: 'Sorted + skip-duplicate trick at same depth prevents duplicate combinations without post-processing.' },
  'nc-76': { approach: 'Backtracking with DFS on the grid. Mark cell as visited (e.g. with #), recurse in 4 directions, unmark on return.', brute: 'O(n×m×4^L) same as optimal — backtracking IS the solution', optimal: 'O(n×m×4^L) where L=word length', why: 'Marking visited in-place avoids O(n×m) extra space. 4^L branches is unavoidable since path can go in any direction.' },
  'nc-77': { approach: 'Backtracking. At each index, try all substrings. If it is a palindrome, add to current partition and recurse. Backtrack.', brute: 'O(n×2^n) — generate all partitions, filter non-palindromes', optimal: 'O(n×2^n) with inline palindrome check — same order, fewer operations', why: 'Pruning: only recurse when current substring is a palindrome. DP precomputation of palindromes reduces check to O(1).' },
  'nc-78': { approach: 'Backtracking. Map each digit to its letters. For each position, try each letter and recurse to next digit.', brute: 'O(4^n × n) — same as backtracking', optimal: 'O(4^n × n) — output-optimal since 4^n combinations possible', why: 'Some digits map to 4 letters (7,9). Output can be 4^n strings each of length n. Cannot do better.' },
  'nc-79': { approach: 'Backtracking. Track occupied columns, diagonal (r-c), anti-diagonal (r+c). Place queen per row, try all columns, skip conflicts.', brute: 'O(n!) — try all column permutations', optimal: 'O(n!) with O(n) conflict check via sets → O(n!) total', why: 'Using sets for diagonal conflict detection reduces per-placement check from O(n) to O(1). Still O(n!) branching.' },
  'nc-80': { approach: 'DFS or BFS from each unvisited land cell. Mark all connected land as visited. Count DFS/BFS starts.', brute: 'O((n×m)²) — for each cell, scan all to find connections', optimal: 'O(n×m) — each cell visited once', why: 'Grid DFS/BFS is O(n×m) since each cell is visited at most once. Union-Find also O(n×m×α) ≈ O(n×m).' },
  'nc-81': { approach: 'DFS from each land cell. Return 0 for water/out-of-bounds. Return 1 + sum of 4 neighbors. Track max area.', brute: 'O((n×m)²) — for each cell, BFS to find component size', optimal: 'O(n×m) — each cell visited once', why: 'Same as number of islands. DFS naturally computes component size. Mark visited in-place to avoid extra space.' },
  'nc-82': { approach: 'DFS/BFS with a HashMap mapping original→copy nodes. Create copy before recursing to handle cycles.', brute: 'O(n²) — for each node, scan all to find and copy neighbors', optimal: 'O(n+e) — visit each node and edge once', why: 'HashMap prevents revisiting and handles cycles. Create clone before recursing ensures the clone exists when neighbors reference it.' },
  'nc-83': { approach: 'Multi-source BFS starting from all gates (value 0) simultaneously. Propagate distance outward.', brute: 'O((n×m)²) — BFS from each gate separately', optimal: 'O(n×m) — multi-source BFS visits each cell once', why: 'Starting BFS from all gates simultaneously means each cell gets the distance to its nearest gate in one pass. Single-source BFS per gate is O(g×n×m).' },
  'nc-84': { approach: 'Multi-source BFS from all rotten oranges. Each level = 1 minute. Track fresh count; if >0 after BFS, return -1.', brute: 'O((n×m)²) — simulate each minute separately', optimal: 'O(n×m) — multi-source BFS, each cell processed once', why: 'Simultaneous spread from all rotten sources in one BFS gives minimum time. Sequential BFS from each source overcounts.' },
  'nc-85': { approach: 'Reverse DFS from ocean borders (Pacific=top/left, Atlantic=bottom/right). Find cells reachable from both oceans.', brute: 'O((n×m)²) — for each cell, DFS to check if it reaches both oceans', optimal: 'O(n×m) — reverse DFS from borders, each cell visited twice max', why: 'Reverse thinking: water flows up from ocean. Cells reachable from both ocean borders (going uphill) are the answer.' },
  'nc-86': { approach: 'DFS from all border O cells, mark as safe. Then flip: unmarked O→X (captured), T→O (border-connected safe).', brute: 'O((n×m)²) — for each O, DFS to check if it touches border', optimal: 'O(n×m) — border DFS marks safe cells in one pass', why: 'Only O cells connected to border are safe. Starting from borders and working inward is O(n×m) vs O(n×m) per interior cell.' },
  'nc-87': { approach: 'Build directed graph. DFS cycle detection: if we reach a node currently in recursion stack, cycle exists → impossible.', brute: 'O(V²+E) — check all paths for cycles', optimal: 'O(V+E) — DFS with visited and in-stack sets', why: 'Topological sort (Kahn\'s BFS) also works. DFS with three states (unvisited, in-stack, done) detects cycles in O(V+E).' },
  'nc-88': { approach: 'Topological sort using Kahn\'s algorithm (BFS with in-degree tracking). Nodes added to result as in-degree reaches 0.', brute: 'O(V²+E) — repeated linear scan for zero in-degree nodes', optimal: 'O(V+E) — BFS queue of zero-in-degree nodes', why: 'Kahn\'s gives ordering naturally. If not all nodes are in result, a cycle exists. DFS postorder reversal also works.' },
  'nc-89': { approach: 'A tree with n nodes has exactly n-1 edges and is connected. Use Union-Find or DFS: check no cycle and all nodes connected.', brute: 'O(n²) — check all edge combinations', optimal: 'O(n×α(n)) Union-Find or O(n+e) DFS', why: 'Union-Find with path compression is practically O(1) per union/find. DFS cycle detection is O(n+e) = O(n) since e=n-1.' },
  'nc-90': { approach: 'Union-Find: union all edges. Count distinct roots. Or DFS: count connected components.', brute: 'O(n²) — check all node pairs', optimal: 'O(n×α(n)) Union-Find or O(n+e) DFS', why: 'Union-Find is elegant for this problem. Each union operation merges components. Final root count = components.' },
  'nc-91': { approach: 'Union-Find. Process edges in order. If both nodes already in same component when adding edge, it is the redundant edge.', brute: 'O(n²) — try removing each edge, check if still a tree', optimal: 'O(n×α(n)) — Union-Find processes each edge once', why: 'The redundant edge is the first edge that creates a cycle. Processing in order with Union-Find finds it in one pass.' },
  'nc-92': { approach: 'BFS level by level. Generate all words one letter apart from current word. Use visited set. Return level when endWord found.', brute: 'O(n×m²×26) — for each word in queue, try all substitutions', optimal: 'O(n×m²×26) same but bidirectional BFS halves the search space', why: 'Each word in queue generates m×26 neighbors checked against dictionary (O(m) hash). Bidirectional BFS is O(b^(d/2)) vs O(b^d).' },
  'nc-93': { approach: 'Build adjacency list with sorted neighbors. DFS (Hierholzer\'s algorithm) — always pick lexicographically smallest next, backtrack if dead end.', brute: 'O(E!) — try all orderings of tickets', optimal: 'O(E log E) — sort neighbors, DFS Euler path', why: 'Euler path problem: visit every edge exactly once. Hierholzer\'s finds it in O(E). Sorting neighbors ensures lexicographic order.' },
  'nc-95': { approach: 'Dijkstra\'s algorithm. Priority queue of (time, node). Relax edges. Return max dist when all nodes reached, or -1.', brute: 'O(V²) — Dijkstra without heap', optimal: 'O((V+E) log V) — Dijkstra with min-heap', why: 'Dijkstra is optimal for single-source shortest paths with non-negative weights. Bellman-Ford O(VE) is used only for negative weights.' },
  'nc-96': { approach: 'Modified Dijkstra: instead of min total distance, minimize the maximum elevation on path. Priority queue of (max_so_far, row, col).', brute: 'O(n² log n) binary search + BFS/DFS', optimal: 'O(n² log n) — Dijkstra with elevation as weight', why: 'This is a minimax path problem. Dijkstra on max-elevation minimizes the bottleneck. Binary search + BFS also works at same complexity.' },
  'nc-97': { approach: 'Build directed graph from adjacent word pairs in sorted dictionary. Topological sort gives character order. Cycle = invalid.', brute: 'O(n²×m) — compare all word pairs', optimal: 'O(C + n×m) where C=unique chars, n×m=total chars across all words', why: 'Only adjacent words in sorted list constrain ordering. Compare consecutive pairs only (not all pairs).' },
  'nc-98': { approach: 'Modified Dijkstra or Bellman-Ford for exactly k stops. State: (cost, node, stops_remaining). Priority queue.', brute: 'O(k×V×E) — Bellman-Ford k iterations', optimal: 'O(E log V) Dijkstra or O(k×(V+E)) BFS/Bellman-Ford', why: 'Standard Dijkstra doesn\'t track stops. State must include stops remaining. Bellman-Ford with k iterations is intuitive and correct.' },
  'nc-99': { approach: 'DP: dp[i] = dp[i-1] + dp[i-2]. Base: dp[1]=1, dp[2]=2. This is Fibonacci. Can optimize to O(1) space with two variables.', brute: 'O(2^n) — naive recursion', optimal: 'O(n) time, O(1) space — two variables', why: 'Overlapping subproblems = DP. Only last two values needed → O(1) space. Mathematical formula (Binet\'s) is O(1) but loses precision.' },
  'nc-100': { approach: 'dp[i] = min(dp[i-1]+cost[i-1], dp[i-2]+cost[i-2]). Return min of last two.', brute: 'O(2^n) recursion', optimal: 'O(n) time, O(1) space', why: 'Only last two dp values needed at any step.' },
  'nc-101': { approach: 'dp[i] = max(dp[i-1], dp[i-2] + nums[i]). Either rob current + skip prev, or skip current. Two variables suffice.', brute: 'O(2^n) recursion', optimal: 'O(n) time, O(1) space', why: 'Optimal substructure: best for house i depends only on i-1 and i-2.' },
  'nc-102': { approach: 'Run House Robber twice: once on nums[0..n-2], once on nums[1..n-1]. Return the max.', brute: 'O(2^n)', optimal: 'O(n) time, O(1) space', why: 'First and last house cannot both be robbed. Split into two linear subproblems.' },
  'nc-103': { approach: 'Expand around center for each character (odd) and each pair (even). Track longest palindrome found.', brute: 'O(n³) — check all substrings', optimal: 'O(n²) time, O(1) space — expand around center', why: 'Manacher\'s algorithm achieves O(n) but is complex for interviews. Expand-around-center is O(n²) and intuitive.' },
  'nc-104': { approach: 'Same expand-around-center technique. Count each palindrome found (+1 per expansion) instead of tracking max length.', brute: 'O(n³) — check all substrings', optimal: 'O(n²) time, O(1) space', why: 'Every center contributes independently. Count while expanding rather than enumerate then check.' },
  'nc-105': { approach: 'DP from right. dp[i] = ways to decode s[i:]. Check single char (1-9) and two chars (10-26). Handle leading zeros.', brute: 'O(2^n) recursion without memoization', optimal: 'O(n) time, O(1) space (two variables)', why: 'Each position depends only on next 1-2 positions. Rolling two variables eliminates full dp array.' },
  'nc-106': { approach: 'DP bottom-up. dp[i] = min coins to make amount i. For each amount, try all coins: dp[i] = min(dp[i], dp[i-coin]+1).', brute: 'O(amount × coins^amount) recursion', optimal: 'O(amount × n) time, O(amount) space', why: 'Unbounded knapsack variant. Each sub-amount solved once. BFS also works and is naturally O(amount × n).' },
  'nc-107': { approach: 'DP tracking curMax and curMin (for negatives). At each element: swap max/min if negative. Update global max.', brute: 'O(n²) — check all subarrays', optimal: 'O(n) time, O(1) space', why: 'Negative flips max to min. Must track both. Kadane\'s for max sum doesn\'t account for sign flips via multiplication.' },
  'nc-108': { approach: 'DP: dp[i] = can s[0:i] be segmented. For each i, check all j<i: if dp[j] and s[j:i] in dict, dp[i]=true.', brute: 'O(2^n) recursion', optimal: 'O(n² × m) time where m=dict lookup cost, O(n) space', why: 'Memoization reduces exponential to polynomial. Trie optimization reduces dict lookup to O(n) total.' },
  'nc-109': { approach: 'DP: dp[i] = LIS ending at i. For each i, check all j<i. Or binary search patience sorting for O(n log n).', brute: 'O(n²) DP', optimal: 'O(n log n) — patience sorting with binary search on tails array', why: 'Maintaining tails array (smallest tail of IS with each length) enables binary search. Each element processed in O(log n).' },
  'nc-110': { approach: 'Target sum = total/2. 0/1 knapsack DP. dp[s] = can we make sum s. Process each num, update dp backward.', brute: 'O(2^n) — try all subsets', optimal: 'O(n × sum/2) time and space; O(sum/2) space with rolling array', why: 'Reduce to subset sum = total/2. 1D DP (iterate backward) avoids using each element twice (0/1 knapsack constraint).' },
  'nc-111': { approach: 'dp[i][j] = dp[i-1][j] + dp[i][j-1]. First row and column = 1. Or use math: C(m+n-2, m-1).', brute: 'O(2^(m+n)) recursion', optimal: 'O(m×n) time, O(n) space with rolling array', why: 'Each cell reached only from left or above. Math formula is O(1) but may overflow. DP is O(m×n) and intuitive.' },
  'nc-112': { approach: 'dp[i][j]: if chars match, dp[i][j]=dp[i-1][j-1]+1; else max(dp[i-1][j], dp[i][j-1]). Reduce to O(n) space with rolling row.', brute: 'O(2^(m+n)) recursion', optimal: 'O(m×n) time, O(min(m,n)) space', why: 'Classic 2D DP. Only previous row needed → O(n) space. Cannot do better than O(m×n) time (proven lower bound).' },
  'nc-113': { approach: 'Three states: held (have stock), sold (just sold), cooldown (resting). Transition: held=max(held, cooldown-price); sold=held+price; cooldown=max(cooldown,sold).', brute: 'O(2^n) try all buy/sell combinations', optimal: 'O(n) time, O(1) space — state machine', why: 'State machine with three states captures all constraints cleanly. Each day transitions in O(1).' },
  'nc-114': { approach: '2D knapsack-style DP. dp[c][a] = ways to make amount a using first c coins. Inner loop forward (unbounded use).', brute: 'O(2^amount) recursion', optimal: 'O(coins × amount) time, O(amount) space', why: 'Unlike Coin Change 1 (min coins), this counts combinations. Forward iteration allows reuse of each coin type.' },
  'nc-115': { approach: 'Reduce to subset sum: count subsets with sum = (total+target)/2. DP counting ways to reach each sum.', brute: 'O(2^n × n) DFS', optimal: 'O(n × sum) DP', why: 'Assigning +/- is equivalent to partitioning into two subsets. Mathematical reduction to subset-sum count avoids exponential DFS.' },
  'nc-116': { approach: 'dp[i][j] = can s3[0:i+j] be formed from s1[0:i] and s2[0:j]. If s1[i-1]==s3[i+j-1], check dp[i-1][j]. Similarly for s2.', brute: 'O(2^(m+n)) recursion', optimal: 'O(m×n) time, O(n) space with rolling row', why: '2D DP captures the two-pointer state through both strings. Rolling array reduces to O(n) space.' },
  'nc-117': { approach: 'DFS with memoization. From each cell, DFS to strictly increasing neighbors. Cache result per cell.', brute: 'O(n×m×4^(n×m)) — DFS without memo', optimal: 'O(n×m) — each cell computed once via memo', why: 'Strictly increasing constraint means no cycles → DFS safe without visited set. Memoization ensures each cell computed once.' },
  'nc-118': { approach: 'dp[i][j] = number of ways s[0:i] contains t[0:j] as subsequence. If s[i]==t[j]: dp[i][j]=dp[i-1][j-1]+dp[i-1][j]; else dp[i][j]=dp[i-1][j].', brute: 'O(2^n) recursion', optimal: 'O(m×n) time, O(n) space rolling', why: 'Classic subsequence DP. The two cases (match/skip) cover all possibilities. Rolling array reduces space.' },
  'nc-119': { approach: 'dp[i][j] = edit distance between word1[0:i] and word2[0:j]. If chars match: dp[i-1][j-1]; else: 1+min(dp[i-1][j], dp[i][j-1], dp[i-1][j-1]).', brute: 'O(3^(m+n)) recursion', optimal: 'O(m×n) time, O(n) space', why: 'Three operations (insert/delete/replace) map to three DP transitions. Rolling row reduces space to O(n).' },
  'nc-120': { approach: 'Interval DP. dp[i][j] = max coins from bursting all balloons between i and j. Try each balloon k as the LAST to burst.', brute: 'O(n!) — try all burst orderings', optimal: 'O(n³) interval DP', why: 'Thinking in reverse (last burst instead of first) gives clean recurrence. Interval DP considers all sub-intervals: O(n²) states × O(n) transitions.' },
  'nc-121': { approach: 'dp[i][j] = does s[0:i] match p[0:j]. Handle * (zero=dp[i][j-2], one=dp[i-1][j] if match) and . (matches any char).', brute: 'O(2^(m+n)) recursion', optimal: 'O(m×n) DP', why: 'Star has two cases: use 0 times (dp[i][j-2]) or use 1+ times (dp[i-1][j] when char matches). DP handles all cases.' },
  'nc-122': { approach: 'Kadane\'s algorithm. curSum = max(num, curSum+num). maxSum = max(maxSum, curSum). Reset when curSum goes negative.', brute: 'O(n²) check all subarrays', optimal: 'O(n) time, O(1) space — Kadane\'s', why: 'Kadane\'s key insight: if running sum goes negative, restart from current element. Provably optimal for max subarray.' },
  'nc-123': { approach: 'Greedy. Track farthest reachable index. If current index > farthest, return false. If farthest >= last index, return true.', brute: 'O(2^n) DFS/backtracking', optimal: 'O(n) time, O(1) space — greedy max reach', why: 'Tracking maximum reachable index is sufficient. No need to simulate each jump — just check if we can ever get stuck.' },
  'nc-124': { approach: 'Greedy BFS levels. Track current range end and next range end. Each time we exhaust current range, we take a jump (increment jumps, update range).', brute: 'O(n²) DP', optimal: 'O(n) greedy — implicit BFS levels', why: 'Greedy BFS processes all positions reachable in k jumps before moving to k+1. Avoids DP\'s O(n²) with O(n) sweep.' },
  'nc-125': { approach: 'If total gas < total cost, impossible. Otherwise, find start: reset start and tank whenever tank goes negative.', brute: 'O(n²) — try each starting station', optimal: 'O(n) — one pass greedy', why: 'If total gas >= total cost, a solution exists and is unique. The start is where the prefix sum last reset to 0.' },
  'nc-126': { approach: 'Sort hand. Use ordered map of counts. For each unique card, try to form a group of groupSize consecutive cards starting from it.', brute: 'O(n!) — try all arrangements', optimal: 'O(n log n) sort + O(n) sweep', why: 'Always start groups from the smallest available card (greedy). Ordered map gives O(log n) access.' },
  'nc-127': { approach: 'Filter valid triplets: a triplet is valid if none of its values EXCEEDS the target in any position. Merge valid triplets by taking max per position. Check if result equals target.', brute: 'O(n³) — try all triples of triplets', optimal: 'O(n) — single pass filter and merge', why: 'A triplet can only help if it doesn\'t violate any target bound. Merging by max is O(1) per triplet, O(n) total.' },
  'nc-128': { approach: 'Track last occurrence of each char. For each char, the partition must end at max(last[char]) for all chars seen so far. When index reaches partition end, record size.', brute: 'O(n²) — for each position scan forward for partition end', optimal: 'O(n) — precompute last occurrence, single pass', why: 'Last-occurrence precomputation is O(n). Greedy partition extension is O(1) per character.' },
  'nc-129': { approach: 'Greedy with two counters: lo (min open brackets) and hi (max open brackets). * increases hi, decreases lo (floor 0). ) decreases both. Valid if lo==0 at end.', brute: 'O(3^n) — try all * substitutions', optimal: 'O(n) — track range of possible open bracket counts', why: 'Instead of trying all * substitutions, track the range [lo, hi] of valid open-bracket counts. If range ever invalid (hi<0), return false.' },
  'nc-130': { approach: 'Iterate intervals. If new interval doesn\'t overlap current, add it. If it does, merge by extending end. Handle left and right non-overlapping parts.', brute: 'O(n²) — insert then merge', optimal: 'O(n) — single pass with three phases', why: 'Three clear phases: add intervals before new one, merge overlapping ones, add after. Single pass is O(n).' },
  'nc-131': { approach: 'Sort by start. Iterate: if current start <= prev end, merge (update end to max). Else start new interval.', brute: 'O(n²) — check all pairs', optimal: 'O(n log n) sort + O(n) merge', why: 'Sorting ensures overlapping intervals are adjacent. Then single pass merges in O(n). Sort dominates at O(n log n).' },
  'nc-132': { approach: 'Sort by end. Greedily keep intervals with earliest end (don\'t remove them). Count removed = total - kept.', brute: 'O(2^n) — try all subsets to remove', optimal: 'O(n log n) sort + O(n) greedy sweep', why: 'Activity selection problem: keeping interval with earliest end maximizes remaining intervals. Equivalent to maximum non-overlapping set.' },
  'nc-133': { approach: 'Sort by start. Check if any consecutive pair overlaps (prev.end > curr.start).', brute: 'O(n²) — check all pairs', optimal: 'O(n log n) sort + O(n) check', why: 'Only adjacent intervals (after sorting) can overlap. One pass after sorting is sufficient.' },
  'nc-134': { approach: 'Sort by start. Min-heap of end times. If new meeting starts after heap top ends, pop (room freed). Push current end. Heap size = min rooms.', brute: 'O(n²) — check all meeting pairs', optimal: 'O(n log n) sort + O(n log n) heap', why: 'Heap tracks earliest ending meeting. If it ends before new meeting starts, reuse room. Heap size at peak = answer.' },
  'nc-135': { approach: 'Sort intervals by start, queries by value. Use min-heap of (size, end). For each query, add all intervals that started. Remove expired. Answer = heap min.', brute: 'O(n×q) — for each query, check all intervals', optimal: 'O((n+q) log n) — sort + heap', why: 'Sweep line: process intervals and queries in order. Heap maintains active intervals sorted by size. Remove expired lazily.' },
  'nc-136': { approach: 'Transpose matrix (swap [i][j] with [j][i]), then reverse each row. This achieves 90-degree clockwise rotation.', brute: 'O(n²) space — create new matrix', optimal: 'O(n²) time, O(1) space — in-place transpose + reverse', why: 'Two simple operations (transpose, reverse rows) compose to a rotation. Both in-place, so O(1) extra space.' },
  'nc-137': { approach: 'Use four boundary pointers (top, bottom, left, right). Traverse edges in order: right, down, left, up. Shrink boundaries each round.', brute: 'O(n×m) same — must visit all cells', optimal: 'O(n×m) time, O(1) extra space', why: 'Boundary shrinking naturally handles all matrix sizes. Must visit all cells, so O(n×m) is optimal.' },
  'nc-138': { approach: 'First pass: record which rows and cols have zeros. Second pass: set those rows/cols to zero. Use first row/col as markers to achieve O(1) space.', brute: 'O(n×m) time, O(n+m) space — store zero positions', optimal: 'O(n×m) time, O(1) space — use matrix itself as marker', why: 'Using first row and column as markers eliminates extra space. Handle first row/col separately with two boolean flags.' },
  'nc-139': { approach: 'Repeatedly sum squares of digits. Detect cycle using Floyd\'s or a HashSet. Happy if reaches 1.', brute: 'O(log n × iterations) with arbitrary termination', optimal: 'O(log n) per step, terminates in bounded steps (cycle length bounded)', why: 'All cycles in unhappy numbers converge to 4. HashSet detects cycle in O(1) per check. Floyd\'s avoids extra space.' },
  'nc-140': { approach: 'Iterate from end. Add 1 with carry. If no carry out of position 0, done. If carry remains, prepend 1.', brute: 'O(n) same — must process all digits in worst case', optimal: 'O(n) time, O(1) extra space (or O(n) for [1,0,0,...0] case)', why: 'Carry propagation stops at first non-9 digit. Worst case [9,9,9] requires O(n) carries and new array.' },
  'nc-141': { approach: 'Repeated squaring. x^n = (x^(n/2))^2. Handle negative n: x^(-n) = 1/(x^n). Handle even/odd n.', brute: 'O(n) — multiply x by itself n times', optimal: 'O(log n) — fast exponentiation by squaring', why: 'Squaring halves the exponent each step → O(log n). Standard divide-and-conquer on the exponent.' },
  'nc-142': { approach: 'Simulate grade-school multiplication. For each digit pair (i,j), product contributes to positions i+j and i+j+1 in result array.', brute: 'O(m×n) same — must multiply all digit pairs', optimal: 'O(m×n) time, O(m+n) space', why: 'Position mapping: digit at i × digit at j contributes to result[i+j] and result[i+j+1]. Handle carry in second pass.' },
  'nc-143': { approach: 'Store point counts in a 2D HashMap. For query point, iterate over all stored points as diagonal-opposite corner. Count valid squares.', brute: 'O(n³) — for each pair of points, check if square exists', optimal: 'O(n²) per query — check each stored point as opposite diagonal corner', why: 'Given query point q and any stored point p with same x or y, compute third and fourth corners. HashMap gives O(1) count check.' },
  'nc-144': { approach: 'XOR all numbers. Pairs cancel out (a XOR a = 0). Single number remains (a XOR 0 = a).', brute: 'O(n) with O(n) space HashSet or sort+scan', optimal: 'O(n) time, O(1) space — XOR', why: 'XOR is self-inverse and commutative. The unique number XORed with all pairs reduces to itself. Elegantly O(1) space.' },
  'nc-145': { approach: 'Brian Kernighan: n &= n-1 removes lowest set bit each iteration. Count iterations. Or use built-in popcount.', brute: 'O(32) — check each bit with mask', optimal: 'O(k) where k = number of 1 bits (Brian Kernighan\'s)', why: 'n & (n-1) clears the lowest set bit. Only k iterations needed vs always 32 for simple bit-by-bit check.' },
  'nc-146': { approach: 'DP: dp[i] = dp[i >> 1] + (i & 1). The number of 1s in i equals 1s in i//2 plus the last bit.', brute: 'O(n × 32) — count bits for each number', optimal: 'O(n) time, O(n) space — DP reusing previously computed values', why: 'Right shift removes last bit, which we already counted. Reusing dp[i>>1] achieves O(n) vs O(32n) naive.' },
  'nc-147': { approach: 'Process 32 bits. For each bit position, take LSB (n & 1), shift into result from left (result |= bit << (31-i)), shift n right.', brute: 'O(32) same — must process all 32 bits', optimal: 'O(32) = O(1) time and space', why: 'Fixed 32-bit integer means O(32) = O(1) always. No better approach possible since all bits must be examined.' },
  'nc-148': { approach: 'XOR all indices 0..n with all values. XOR of (0..n) XOR all nums cancels pairs, leaving missing number. Or: sum formula n*(n+1)/2 - sum(nums).', brute: 'O(n log n) sort + scan or O(n) HashSet', optimal: 'O(n) time, O(1) space — XOR or math formula', why: 'XOR approach: pairs cancel, missing number survives. Math: expected sum minus actual sum. Both O(n) time, O(1) space.' },
  'nc-149': { approach: 'Use bit manipulation to add without +. XOR gives sum without carry. AND shifted left gives carry. Repeat until no carry.', brute: 'Not applicable — + and - are forbidden', optimal: 'O(1) — at most 32 iterations (32-bit integers)', why: 'XOR = sum without carry. AND<<1 = carry. Repeated application simulates full addition in O(1) for fixed-width integers.' },
  'nc-150': { approach: 'Build reversed integer digit by digit. Check overflow before each multiplication: if rev > (INT_MAX - digit) / 10, return 0.', brute: 'Convert to string, reverse, convert back — fails on overflow', optimal: 'O(log x) — number of digits', why: 'String conversion handles reversal but overflow check is complex. Math approach checks overflow before it happens.' },
};
